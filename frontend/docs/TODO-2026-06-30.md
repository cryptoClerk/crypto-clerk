# CryptoBooks — Today's Todo List (2026-06-30)

**Goal:** Get the live app fully working end-to-end.

---

## 🔴 Critical (Must Do)

### 1. Run Database Migration
**Why:** The app crashes because Supabase has no tables. This is the #1 blocker.
**What to do:**
```bash
cd /Users/argylebot/.openclaw/workspace/cryptobooks/frontend
DATABASE_URL="postgresql://postgres:***@db.zjembkhlhlkvmtwplwqu.supabase.co:5432/postgres?sslmode=require" npx prisma migrate deploy
```
**If that fails (VPN):**
- Go to https://app.supabase.com → your project → SQL Editor
- Paste the migration SQL (I can generate it for you)
- Run it

### 2. Verify Live Site Works
**After migration:**
- Visit https://crypto-clerk.onrender.com
- Check /api/health returns `{"status": "healthy"}`
- Test generating a receipt with a real Ethereum tx hash

---

## 🟡 Important (Should Do)

### 3. Get Supabase Auth Keys
**Why:** The auth scaffolding is written and ready. Just needs keys.
**What to do:**
- Go to https://app.supabase.com → your project → Project Settings → API
- Copy:
  - `SUPABASE_URL` (the URL)
  - `SUPABASE_ANON_KEY` (public key, starts with `eyJ...`)
- Add to Render dashboard Environment Variables
- Redeploy or wait for auto-deploy

### 4. Test Real Blockchain Data
**Why:** Etherscan integration is done but untested on live site.
**What to do:**
- Go to https://etherscan.io
- Find a recent USDC transfer tx (e.g., any `Transfer` event)
- Paste the tx hash into the live app's receipt generator
- Verify it fetches real data (not mock data)

### 5. Fix Render Auto-Deploy
**Why:** Currently auto-deploy might be failing silently.
**What to do:**
- Go to Render dashboard → your service
- Check latest deploy logs
- If red/failed, hit "Manual Deploy" → "Clear Build Cache & Deploy"
- Verify deploy succeeds

---

## 🟢 Nice to Have (If Time)

### 6. Rotate Supabase Password
**Why:** You shared it in Discord chat. Security best practice.
**What to do:**
- Supabase dashboard → Database → Reset password
- Update Render env var `DATABASE_URL` with new password

### 7. Set Up Stripe Account
**Why:** Payment scaffolding is written and ready.
**What to do:**
- Go to https://stripe.com
- Create account
- Create two products: Pro ($19/mo) and Business ($49/mo)
- Copy `STRIPE_SECRET_KEY` and price IDs
- Add to Render env vars

### 8. Test Auth Flow
**After adding Supabase keys:**
- Try signing up with email/password on live site
- Verify user appears in Supabase Auth dashboard
- Verify receipts are scoped to that user (not just IP)

---

## 📋 Quick Reference

**Live URL:** https://crypto-clerk.onrender.com  
**GitHub:** https://github.com/cryptoClerk/crypto-clerk  
**Render Dashboard:** https://dashboard.render.com  
**Supabase Dashboard:** https://app.supabase.com

**Env Vars Needed on Render:**
- `DATABASE_URL` ✅ (already set)
- `ETHERSCAN_API_KEY` ✅ (already set)
- `ALLOWED_ORIGIN` ✅ (already set)
- `NEXT_PUBLIC_SUPABASE_URL` ⬜ (needs adding)
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` ⬜ (needs adding)
- `STRIPE_SECRET_KEY` ⬜ (optional, Phase 3)

---

**When you're home, just say "let's start" and I'll walk you through #1.**
