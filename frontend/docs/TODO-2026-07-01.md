# CryptoBooks — Today's Todo List (2026-07-01)

**Goal:** Build statement generation + test auth. Ship MVP features while JB is remote.

---

## ✅ Yesterday's Status (COMPLETE)

| # | Task | Status |
|---|------|--------|
| 1 | Database migration | ✅ Supabase tables live |
| 2 | Live site works | ✅ https://crypto-clerk.onrender.com |
| 3 | Supabase auth keys | ✅ Added to Render, auto-deploying |
| 4 | Real blockchain data | ✅ Etherscan V2 fixed, USDC receipts work |
| 5 | Render auto-deploy | ✅ Pushing to main triggers deploy |
| 6 | Custom domain | ✅ Confirmed on Render |

---

## 🔴 Today — Critical (Argyle can do solo)

### 1. Test Auth Flow (After deploy finishes)
**What:** Verify Supabase auth works on live site
**How:**
- Wait for Render deploy to finish (~2-3 min after env var save)
- Test signup on live site
- Check user appears in Supabase Auth dashboard
- If broken → fix and redeploy

### 2. Build Statement Generation
**What:** Date range + wallet → PDF statement from blockchain history
**How:**
- Reuse Etherscan `tokentx` endpoint (already working)
- UI: Date picker + wallet address input
- PDF: Monthly summary with tx list, totals, running balance
- API route: `/api/statements/generate`

### 3. Build Invoice Creation
**What:** Convert receipt → invoice with line items, client info, due date
**How:**
- UI: "Convert to Invoice" button on receipt page
- Add fields: Client name, description, due date, notes
- PDF template: Professional invoice layout
- API route: `/api/invoices/create`

---

## 🟡 Today — Important (Needs JB later)

### 4. Stripe Setup (Phase 3 — when JB has 15 min)
**What:** Enable Pro/Business plan payments
**Needs from JB:**
- Create Stripe account
- Create products: Pro ($19/mo), Business ($49/mo)
- Copy `STRIPE_SECRET_KEY` + price IDs
- Add to Render env vars

### 5. Supabase Password Rotation (Declined — do later)
**Status:** JB declined for now. Revisit when convenient.

---

## 📋 Quick Reference

**Live URL:** https://crypto-clerk.onrender.com  
**GitHub:** https://github.com/cryptoClerk/crypto-clerk  
**Render Dashboard:** https://dashboard.render.com  
**Supabase Dashboard:** https://app.supabase.com

**Env Vars on Render (Current):**
- `DATABASE_URL` ✅
- `ETHERSCAN_API_KEY` ✅
- `ALLOWED_ORIGIN` ✅
- `NEXT_PUBLIC_SUPABASE_URL` ✅ (just added)
- `NEXT_PUBLIC_SUPABASE_ANON_KEY` ✅ (just added)
- `STRIPE_SECRET_KEY` ⬜ (Phase 3)

---

**When JB is back:** Test auth flow together, decide on Stripe priority, review statement/invoice features.

**Argyle's focus today:** Build statement generation + invoice creation while auth deploys.
